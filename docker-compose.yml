version: '3.8'

services:
  agreement4-be:
    build: .
    container_name: agreement4-be
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
    networks:
      - agreement4-network

networks:
  agreement4-network:
    driver: bridge
